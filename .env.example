# =========================================================
# MaTriX-AI: Full Stack Environment Variables Reference
# =========================================================

# ---------------------------------------------------------
# [1] SUPABASE & POSTGRES DATABASE
# ---------------------------------------------------------
# Connection string from your Supabase dashboard > Database Settings
# MUST include "?sslmode=require" at the end if hosting remotely.
# MUST have the `pgvector` extension enabled in Supabase SQL Editor:
#   CREATE EXTENSION IF NOT EXISTS vector;
DATABASE_URL="postgresql://postgres.xxx:YOUR_PASSWORD@aws-0-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require"

# ---------------------------------------------------------
# [2] FRONTEND (Next.js / Vercel)
# ---------------------------------------------------------
# Address where the EDGE node is deployed (Render URL)
NEXT_PUBLIC_API_URL="https://matrix-edge-xxx.onrender.com"
NEXT_PUBLIC_APP_NAME="MaTriX-AI Dashboard"
NEXT_PUBLIC_APP_VERSION="1.0-RC1"

# ---------------------------------------------------------
# [3] EDGE NODE (FastAPI / Render)
# ---------------------------------------------------------
# Uses the DATABASE_URL defined above
# Needs the cloud API gateway URL
CLOUD_API_URL="https://matrix-cloud-xxx.onrender.com"

# The shared secret key passed to the cloud node for authentication
# Auto-generated by Render Blueprint (render.yaml), or enter your own:
CLOUD_API_KEY="super_secure_random_key_123"

# JWT Auth Secret for Frontend <-> Edge tokens
# Auto-generated by Render Blueprint, or generate via `openssl rand -hex 32`
JWT_SECRET_KEY="your_32_char_hex_secret"
DEMO_CLINIC_PASSWORD="demo"

# ---------------------------------------------------------
# [4] CLOUD NODE (FastAPI / Render)
# ---------------------------------------------------------
# Also uses the CLOUD_API_KEY for verifying edge requests
# Cloud requires the HuggingFace fallback if SageMaker dies
HF_API_TOKEN="hf_xxxxxxxxxxxxxxxxxxxxxxxxxxx"

# (Optional) Amazon SageMaker integration for the real 27B deployment
AWS_ACCESS_KEY_ID="AKIAxxxxxxxx"
AWS_SECRET_ACCESS_KEY="xxxxxxxxxxxxxxxxx"
AWS_REGION="us-east-1"
