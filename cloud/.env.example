CLOUD_API_KEY=changeme

# HuggingFace Inference Endpoint (AWS) — fill in after deploying Gemma-2-27B-IT
HF_INFERENCE_ENDPOINT=https://YOUR-ENDPOINT-ID.us-east-1.aws.endpoints.huggingface.cloud
HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
HF_MODEL_ID=google/gemma-2-27b-it

# AWS SageMaker — Alternative to HuggingFace Endpoint
SAGEMAKER_ENDPOINT_NAME=medgemma-27b-endpoint
AWS_REGION=us-east-1

# Ollama fallback (used if SageMaker and HF are not configured)
OLLAMA_BASE_URL=http://localhost:11434
CLOUD_MODEL=llama3:latest

DEBUG=false
